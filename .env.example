# Required: Anthropic API key (primary LLM)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: OpenAI API key (fallback LLM)
OPENAI_API_KEY=

# Optional: Default GitHub token used for all repos (users may override per-mission for private repos)
GITHUB_TOKEN=
